apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: ai-agent
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
